name: 'finlake_dbt'
version: '1.0.0'
config-version: 2
profile: 'finlake_dbt'
model-paths: ['models']
test-paths: ['tests']
target-path: 'target'
clean-targets: ['target', 'dbt_packages']
models:
  finlake_dbt:
    staging:
      +materialized: view
      +schema: DBT_STAGING
    marts:
      +materialized: table
      +schema: DBT_MARTS
